# Must point at a local demos database.
DATABASE_URL=postgresql://localhost:1234/etc
APPROVED_DEMO_GRAPHQL_ENDPOINT=http://localhost:4000/graphql
# Provide either full cookie header (e.g. "id_token=...; access_token=...")
APPROVED_DEMO_GRAPHQL_COOKIE=
# Or provide only id_token and script will build the cookie header.
APPROVED_DEMO_ID_TOKEN=
# Convenience helper usage:
#   npm run create:demo:set-auth -- --cookie-file ./path/to/cookies.txt
#   npm run create:demo:set-auth -- --cookie-header "id_token=...; access_token=..."
#   npm run create:demo:set-auth -- --id-token "<jwt>"
UPLOAD_BUCKET=upload-bucket
CLEAN_BUCKET=clean-bucket
DELETED_BUCKET=deleted-bucket
S3_ENDPOINT_LOCAL=http://localstack:4566

APPROVED_DEMO_SDG_DIVISION="Division of System Reform Demonstrations"
# Will need a doc for submitting, this is a default one
APPROVED_DEMO_DOCUMENT_PATH="./assets/test_doc.pdf"
APPROVED_DEMO_PROJECT_OFFICER_USER_ID=00000000-0000-0000-0000-000000000000
