// Flags: STATIC_CONSTRAINT
model ApplicationPhaseTypeLimit {
  id String

  phase             Phase              @relation(fields: [id], references: [id])
  applicationPhases ApplicationPhase[]

  @@id([id])
  @@map("application_phase_type_limit")
}
