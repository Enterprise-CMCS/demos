// Flags: TYPE_LIMITER
model CmsUserPersonTypeLimit {
  id String

  personType      PersonType       @relation(fields: [id], references: [id])
  deliverables    Deliverable[]
  privateComments PrivateComment[]

  @@id([id])
  @@map("cms_user_person_type_limit")
}
