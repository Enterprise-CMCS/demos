// Flags: STATIC_CONSTRAINT
model GrantLevel {
  id String

  permissions                  Permission[]
  roles                        Role[]
  systemGrantLevelLimit        SystemGrantLevelLimit?
  demonstrationGrantLevelLimit DemonstrationGrantLevelLimit?

  @@id([id])
  @@map("grant_level")
}
