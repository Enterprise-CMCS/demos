// Flags: STATIC_CONSTRAINT
model GrantLevel {
  id String

  permissions                      Permission[]
  roles                            Role[]
  systemGrantLevelTypeLimit        SystemGrantLevelTypeLimit?
  demonstrationGrantLevelTypeLimit DemonstrationGrantLevelTypeLimit?

  @@id([id])
  @@map("grant_level")
}
