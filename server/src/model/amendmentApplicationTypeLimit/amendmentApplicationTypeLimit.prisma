// Flags: NO_CREATED_TS, NO_UPDATED_TS, NO_HISTORY, NATURAL_KEY
// Table is a static constraint and is updated entirely via migrations
model AmendmentApplicationTypeLimit {
  id String @id

  applicationType ApplicationType @relation(fields: [id], references: [id])
  amendments      Amendment[]

  @@map("amendment_application_type_limit")
}
