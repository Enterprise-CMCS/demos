model ApplicationPhase {
  applicationId String   @map("application_id") @db.Uuid
  phaseId       String   @map("phase_id")
  phaseStatusId String   @map("phase_status_id")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz()
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz()

  application Application               @relation(fields: [applicationId], references: [id])
  phase       ApplicationPhaseTypeLimit @relation(fields: [phaseId], references: [id])
  phaseStatus PhaseStatus               @relation(fields: [phaseStatusId], references: [id])

  @@id([applicationId, phaseId])
  @@map("application_phase")
}
