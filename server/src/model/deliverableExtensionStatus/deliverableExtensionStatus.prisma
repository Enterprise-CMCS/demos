// Flags: STATIC_CONSTRAINT
model DeliverableExtensionStatus {
  id String

  deliverableExtensions                 DeliverableExtension[]
  deliverableActiveExtensionStatusLimit DeliverableActiveExtensionStatusLimit[]

  @@id([id])
  @@map("deliverable_extension_status")
}
