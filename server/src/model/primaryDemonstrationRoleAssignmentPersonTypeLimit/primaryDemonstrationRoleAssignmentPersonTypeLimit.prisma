// Flags: TYPE_LIMITER
model PrimaryDemonstrationRoleAssignmentPersonTypeLimit {
  id String

  personType                          PersonType                           @relation(fields: [id], references: [id])
  primaryDemonstrationRoleAssignments PrimaryDemonstrationRoleAssignment[]

  @@id([id])
  @@map("primary_demonstration_role_assignment_person_type_limit")
}
