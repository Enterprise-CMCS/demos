// Flags: STATIC_CONSTRAINT
model DocumentType {
  id String

  phaseDocumentTypes                         PhaseDocumentType[]
  budgetNeutralityWorkbookDocumentTypeLimits BudgetNeutralityWorkbookDocumentTypeLimit[]

  @@id([id])
  @@map("document_type")
}
