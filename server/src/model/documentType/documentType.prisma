// Flags: STATIC_CONSTRAINT
model DocumentType {
  id String

  phaseDocumentTypes                        PhaseDocumentType[]
  budgetNeutralityWorkbookDocumentTypeLimit BudgetNeutralityWorkbookDocumentTypeLimit[]

  @@id([id])
  @@map("document_type")
}
