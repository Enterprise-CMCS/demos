model Permission {
  id              String           @id
  grantLevelId String

  grantLevel GrantLevel @relation(fields: [grantLevelId], references: [id])
  rolePermissions RolePermission[]

  @@unique([id, grantLevelId])
  @@map("permission")
}
