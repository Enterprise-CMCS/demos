// Flags: STATIC_CONSTRAINT
model Phase {
  id          String
  phaseNumber Int    @map("phase_number")

  bundlePhaseStatuses BundlePhaseStatus[]
  demonstrations      Demonstration[]
  modifications       Modification[]

  @@id([id])
  @@unique([phaseNumber])
  @@map("phase")
}
