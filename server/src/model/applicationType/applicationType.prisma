// Flags: NO_CREATED_TS, NO_UPDATED_TS, NO_HISTORY, NATURAL_KEY
// Table is a static constraint and is updated entirely via migrations
model ApplicationType {
  id String @id

  applications                  Application[]
  demonstrationApplicationTypes DemonstrationApplicationTypeLimit[]
  modificationApplicationTypes  ModificationApplicationTypeLimit[]

  @@map("application_type")
}
