// Flags: TYPE_LIMITER
model DemonstrationApplicationTypeLimit {
  id String @id

  applicationType ApplicationType @relation(fields: [id], references: [id])
  demonstrations  Demonstration[]

  @@map("demonstration_application_type_limit")
}
