/**
 * Type declarations for build-time constants.
 * These are injected via esbuild's define option and should be in sync
 * with the constants defined in scripts/build.mjs.
 */

declare const __DEMOS_VERSION__: string;
declare const __FEATURE_PRINT_VERSION__: boolean;
