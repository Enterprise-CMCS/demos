= DEMOS data migration: overview
:toc: left

This is the documentation set for the DEMOS data migration -- the
project that moves program data off the legacy PMDA system and onto the new
database that backs DEMOS. Go-live is planned for
*Thursday, August 13, 2026*.

This page is the high-level briefing. Use the timeline below to see
where we are, then jump into the audience book that matches your role.

== At a glance

* *Goal.* Replace the legacy PMDA MySQL database with a new PostgreSQL database that
  backs the new DEMOS application, with no loss of meaningful data and no
  surprises for end users.
* *Approach.* Stand the new system up alongside the old one, check it
  against the SME-defined source of truth, rehearse the switch twice, and cut over
  in a planned window.
* *Go-live.* Thursday, *2026-08-13*.
* *Where we are today (2026-07-30).* The application layer is built. Of the 19
  target tables in the scope ledger
  (`sql/99_parity/30_scope_coverage.sql`), 16 are BUILT, one is PARTIAL
  (program-detail tag assignment, pending an SME mapping expansion), one is
  DEFERRED post-MVP (extensions/renewals), and one is OUT-OF-SCOPE by operator
  decision (documents). Two Tier-1 rehearsals ran on 2026-07-22 against
  PROD-scale data; the second cleared the amendment-status and chip_id-mint
  findings from the first and left three REDs, all pre-existing source-data
  row-count findings. A full Tier-2 rehearsal, taking the flip / smoke / decom
  path end to end, has not been run yet.

== Timeline (50,000-foot view)

The work is grouped into three phases. The detailed week-by-week
breakdown -- with exit gates and engineering steps -- lives in the
xref:spec/canonical-spec.adoc[canonical spec].

[mermaid, width=1600]
....
gantt
  title DEMOS migration -- go-live Thu 2026-08-13 (status 2026-07-30)
  dateFormat  YYYY-MM-DD
  axisFormat  %b %d
  excludes    weekends
  todayMarker stroke-width:3px,stroke:#d33,opacity:0.7

  section Build
  Foundation & design                    :done, p1, 2026-05-04, 10d
  Move & reshape the data                :done, p2, after p1, 50d

  section Validate
  Tier-1 rehearsals (script shakedown)   :done, p4a, 2026-07-22, 1d
  Integration & user testing             :active, p3, 2026-07-27, 10d
  Tier-2 rehearsal (full cutover path)   :p4b, 2026-08-06, 4d

  section Go-live
  Final prep                             :pre, 2026-08-12, 1d
  Go-live (Thu 2026-08-13)               :crit, milestone, golive, 2026-08-13, 0d
  Hypercare support                      :hc, after golive, 4d
  Retire legacy system                   :dc, after hc, 3d
....

.What each phase delivers
[cols="1,3"]
|===
| Phase | Done when...

| Foundation & design
| The new database is structured and the major data-shape decisions
  are signed off.

| Move & reshape the data
| Every record from the legacy system has been carried over and
  reshaped so the new application can read it.

| Tier-1 rehearsals
| The build path runs clean against PROD-scale data. Script shakedown
  only: no flip, smoke, or decom.

| Integration & user testing
| DEMOS runs against the new database and clears a full
  round of user-acceptance testing with no severe issues open.

| Tier-2 rehearsal
| The full switch-over procedure has been timed end to end, with no
  surprises and a validated rollback.

| Go-live & hypercare
| The new system is in production, supported through a four-day
  hypercare window, with legacy PMDA queued for retirement.
|===

.How to read the bars
[cols="1,3"]
|===
| Marker | Meaning

| Green
| Phase completed.

| Filled (active)
| Phase currently in flight.

| Open
| Phase not yet started.

| Red diamond
| Go-live milestone (Thu 2026-08-13).

| Vertical red line
| Today.
|===

== How to read

* If you are running the cutover today, start with the
  xref:operator/index.adoc[operator book].
* If you are extending the pipeline (SQL, Python, pgloader), start with
  the xref:developer/index.adoc[developer book].
* If you are reviewing data-mapping decisions, start with the
  xref:sme/index.adoc[SME book].
* If you want the end-to-end plan in one document, read the
  xref:spec/canonical-spec.adoc[canonical spec].

NOTE: The build is multi-page: each `.adoc` file under `spec/`, `operator/`,
`developer/`, and `sme/` becomes its own `.html` at the matching
relative path under `build/`. Open `build/README.html` to enter; the left-hand TOC of
each page navigates within that page, the body links navigate between
pages.

// docnav-start
ifndef::nested[]
'''

[.docnav,cols="1,1,1",frame=none,grid=none,stripes=none]
|===
|
^|xref:toc.adoc[Table of contents]
>|xref:spec/canonical-spec.adoc[next >]
|===
endif::[]
// docnav-end
